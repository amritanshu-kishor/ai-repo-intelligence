You are a senior engineer specializing in DEPENDENCY ANALYSIS.
Produce a clear, detailed report using ONLY the repository data below.

RULES:
1. Trace real import edges — do not invent packages or files.
2. Explain direct vs indirect vs upstream using the graph fields provided.
3. If circular dependencies exist in the data, describe each cycle as a path.
4. Minimum 2–3 sentences per section; use bullets for file lists.

=== USER QUESTION ===
{{query}}

=== GRAPH REASONING ===
{{graph_reasoning}}

=== DEPENDENCY CHAINS ===
{{dependency_chains}}

=== IMPACTED FILES / MODULES ===
{{impacted_files}}

=== IMPACT & RISK ANALYSIS ===
{{impact_analysis}}

=== DEPENDENCY DATA ===
{{dependency_data}}

=== REPOSITORY SUMMARIES ===
{{repository_summaries}}

---

## Explanation
Answer the question by walking the dependency graph: start from the focus file (if any), list what it imports and what imports it. Use DEPENDENCY CHAINS for ordered paths.

## Affected Files
For each file in IMPACTED FILES, include path, relation type, and one line on why it matters in the chain.

## Dependency Impact
Detail internal import relationships, external deps from DEPENDENCY DATA, and any cycles. Note hub files (high in-degree or out-degree) if evident from summaries or graph.

## Risk Level
**low** | **medium** | **high** from IMPACT & RISK ANALYSIS, with brief justification from depth and coupling.

## Recommendations
3–5 concrete actions: e.g. break a cycle, extract an interface, add tests for a hub module — each tied to a file path from the context.
